name: Web E2E Tests (Playwright)

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]

jobs:
    web-e2e-tests:
        name: Web E2E Tests (Playwright)
        runs-on: ubuntu-latest
        defaults:
            run:
                shell: bash

        env:
            API_PORT: 4000
            WEB_PORT: 3000
            NODE_ENV: test

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: "npm"
                  cache-dependency-path: |
                      ./api/package-lock.json
                      ./web/package-lock.json

            - name: Install API dependencies
              working-directory: ./api
              run: npm ci

            - name: Install Web dependencies
              working-directory: ./web
              run: npm ci

            - name: Install Playwright browsers
              working-directory: ./web
              run: npx playwright install --with-deps

            - name: Build web app
              working-directory: ./web
              run: npm run build

            - name: Start API and Web servers
              run: |
                  cd api && PORT=$API_PORT npm run dev &
                  cd web && PORT=$WEB_PORT npm run start &
                  npx wait-on http://localhost:$API_PORT/ping
                  npx wait-on http://localhost:$WEB_PORT

            - name: Run Playwright E2E tests
              working-directory: ./web
              run: npx playwright test --reporter=dot

            - name: Upload Playwright report (if failed)
              if: failure()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-report
                  path: web/playwright-report/
